# Optional Custom Domain

Only add a root-level `CNAME` file if you own a domain and want to connect it to GitHub Pages.

Example CNAME content:

```text
releases.tizwildin.com
```

Do not add this until DNS is configured.
